# 设计文档

固定路径塔防，一局 10 波。核心是**属性克制**：打对属性和打错属性的伤害差到 8.7 倍，
不靠克制基本打不过（模拟实测，见下方「平衡结论」）。

## 一、核心玩法

进游戏先看到**标题页**（开始游戏／说明／退出）。

一局 = 10 波怪。**第一波之前先送 3 次三选一**——开局就让玩家做三次选择，
爽感前置，而不是先打一波空白的怪。

每波之间有建造阶段，可以看到**下一波的属性构成**再决定怎么建。
**波次进行中一样能建塔、升级、拆除**——赏金即时到账，打到一半发现属性配错了可以当场补救，
而不是干看著动画跑完。
每波打完给 **3 次 3 选 1** 的奖励（含开局共 30 次），奖励可重复领取、效果累加。
每一轮三选一都可以**刷新**：第一次免费，之后 1、2、3… 金递增，每轮重置。
命归零就输，打完 10 波就赢。目前只做单局，没有跨局解锁。

## 二、属性克制

猜拳三角：**火 → 木 → 水 → 火**。

| 塔 vs 怪 | 倍率 | 说明 |
|---|---|---|
| 克制 | **2.6** | 火打木、木打水、水打火 |
| 同属性 | 0.55 | |
| 被克制 | **0.3** | 反著打，几乎打不动 |
| 无属性塔打有属性怪 | **0.3** | 见下方「歪路封堵」 |

克制／被克制的倍率差是 **8.7 倍**，这条写进了回归测试（`test_core.gd`），
低于 5 倍就红——差距不够大的话玩家不会在意属性，整个机制就白做了。

## 三、塔

**没有射程。** 所有塔盖在画面底部，打得到全场任何怪。
这是刻意的取舍：砍掉「位置经营」，让决策全部集中在属性配比和资源分配上。

- 塔位上限 **8** 个
- 初始只能造**无属性塔**，可花钱升级成火／木／水之一
- **升级后不能再改属性**（火塔不能变水塔），只能拆掉重建
- 属性塔还能继续**练级**，每升一级多打一个目标（全额伤害），上限 Lv4
- **每波可免费拆除 1 次**（全额返还），之后每次拆除只返还 60%
- 单点攻击，目标固定是「走得最前面的活怪」
- 基础伤害 10，攻速 1.2 次/秒

### 塔位定价

第 slot 号塔位价格 = `60 × 1.45^slot` → 60 / 87 / 126 / 183 / 265 / 384 / 557 / 808，八座共 2470。

**价格绑在塔位上，不是绑在「已有几座塔」上。** 这个区别很要命：
按塔数定价时，拆掉早期的便宜塔、原位重建却要付最贵那一档的价，白亏几百块，
属性重组在数值上根本不成立。绑塔位之后，拆了原位重建刚好不赚不亏。

递增本身也是模拟逼出来的：塔如果固定 60 块，**第 3 波就能把 8 个塔位填满**，
之后金钱完全失去意义（实测结局剩 2478 金没地方花）。
涨价之后，「要不要开第 6 座塔」才变成一个真决策。

### 金额一律取整

所有玩家看得到的花费都过一次 `Balance.round_cost()`，取整到 10 的倍数。
87 金、143 金这种数字在界面上很难读，取整之后玩家一眼就能估「我还差几座塔」。

塔位：**60 / 90 / 130 / 180 / 270 / 380 / 560 / 810**
升级成属性塔：**60**　练级：**160 / 410 / 1050**

### 练级

| 等级 | 同时打几个目标 |
|---|---|
| Lv1（刚升成属性塔） | 1 |
| Lv2 / Lv3 / Lv4 | 2 / 3 / 4 |

价格 = `55 × 2.6^(n-1)` → **Lv2 143 / Lv3 371 / Lv4 966**。

**这是目前对难度影响最大的单一参数**，比血量成长灵敏得多：

| 涨价幅度 | 1.8 | 2.6 | 4.0 | 5.0 |
|---|---|---|---|---|
| 最强打法通关率 | 70.7% | **50.0%** | 39.2% | 30.0% |

定在 2.6 是为了把通关率放在 50% 左右，顺带让 Lv4 真的练得到——
4.0 的时候 Lv4 要 3520 金，一局下来基本没人碰得到，等于死内容。

> 顺带一提，压「等级上限」没用：Lv3 上限反而比 Lv4 通关率更高（60.7% vs 55.3%），
> 因为省下来的钱会跑去多盖塔。要控难度就得动价格，不是动上限。

## 四、技能

**同属性凑满 3 座塔**就解锁该属性的技能。手动施放，有冷却（18 秒，一波大概能放 1~2 次），
每波开场冷却归零。

| 属性 | 效果 |
|---|---|
| 火 | 全屏火系 AOE + **灼烧**，每 0.2 秒跳一次伤害，持续 3 秒 |
| 水 | 全屏水系 AOE + **减速 50%**，持续 4 秒 |
| 木 | 全屏木系 AOE + **缠绕**，完全定身 2.5 秒 |

伤害 = `26 × 该属性所有塔的等级总和`，**再乘属性克制倍率**。

两个设计要点：

1. **挂钩等级总和而不是塔数。** 三座 Lv1 和三座 Lv4 的技能威力差四倍，
   「练精一种属性」才有回报，否则玩家只会铺三座 Lv1 凑解锁条件。
2. **技能伤害一样吃克制。** 火技能打木怪 ×2.6、打水怪 ×0.3——
   技能不是绕过克制机制的后门，而是克制机制的延伸。

## 五、经济

| 项目 | 数值 |
|---|---|
| 开局金钱 | 220（只够 2 座塔 + 1 次升级） |
| 开局生命 | 20 |
| 升级费用 | 55（固定，不随塔数涨） |
| 击杀赏金 | 6 + 2×波次 |
| 过波奖金 | 40 |
| 漏怪扣命 | 杂兵 1，精英 5 |

## 六、波次

- 第 n 波数量 = `6 + 1.6n`（第 1 波 8 只，第 10 波 22 只）
- 第 n 波单只血量 = `55 × 1.52^(n-1)`
- 属性构成：第 1–2 波单一属性，第 3–6 波两种，第 7–10 波三种混合
- 第 5、10 波追加精英怪：血量 ×6，赏金 ×5，移速 ×0.65
- 赛道长 24 单位，怪速 1.7（约 14 秒走完），每波速度 +2%

## 七、奖励池（16 种，可重复领取）

奖励分 **S / A / B / C** 四个稀有度，稀有度**同时决定卡片颜色和抽中权重**
（C 100、B 55、A 28、S 9）。S 如果跟 C 一样常见，那个 S 就不值钱了。

| 稀有度 | 名称 | 效果 |
|---|---|---|
| C | 淬火 | 全体塔伤害 +12% |
| C | 急速 | 全体塔攻速 +10% |
| C | 修补 | 生命 +3 |
| C | 赏金 | 立刻获得 60 + 20×波次 金 |
| B | 贯穿之刃 | 每次命中附加 6 点真伤（无视属性） |
| B | 韧性 | 被克制惩罚减轻 +0.10（封顶 0.7） |
| B | 本源 | 无属性打有属性 +0.12（封顶 0.9） |
| B | 利息 | 每波结束按存款给 8%（封顶 30%） |
| B | 精炼 | 升级与练级费用 −20%（封顶 −60%） |
| A | 锐利 | 克制倍率 +0.25（封顶 +1.0） |
| A | 凝滞 | 命中附带 20% 减速 1.5 秒（封顶 60%） |
| A | 穿透 | 额外打到后方 1 只，造成 40% 伤害 |
| A | 免费升级券 | 免费把一座无属性塔升成任意属性 |
| A | 蓄能 | 技能冷却 −15%（封顶 −50%） |
| S | 破绽 | 暴击**阶梯式**成长，见下 |
| S | 爆发 | 技能伤害 +25% |

所有加成都是**加算**不是乘算，并且关键栏位有封顶——27 次选择如果用乘算会直接爆表。

### 三条从实际试玩改出来的规则

**1. 不放有负面效果的奖励。**
原本有「◯之专精：该属性 +35%，另外两系 −15%」，但玩家一看到负数基本就不选了，
等于那一格被浪费掉。已移除。

**2. 有封顶的奖励必须显示现在叠到多少。**
每张卡片第三行写「目前 0.30 / 封顶 0.70」——没有这行，玩家会在已经封顶之后还继续选。

**3. 暴击是阶梯式的，不是固定 2 倍。**

| 领取次数 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 机率 | 15% | 25% | 35% | 45% | 55% |
| 倍率 | ×2.0 | ×2.3 | ×2.6 | ×2.9 | ×3.2 |

固定 2 倍的暴击领到第三次就索然无味了。机率和倍率一起长，才会有「越堆越猛」的感觉。
（机率封顶 65%，倍率不封顶。）

> **和「不能改属性」的冲突：** 原本设计的「属性切换券」（免费把火塔改成水塔）
> 直接违反硬规则，已改成「免费升级券」，只作用于还没升级的无属性塔。

## 八、平衡结论（模拟实测）

全部数据来自 `tests/sim/`，脚本玩家 `GreedyPlayer` 跑 120～300 局，固定种子可复现。

### 最终参数的表现

脚本玩家 `GreedyPlayer` 跑 300 局的成绩：

| 打法 | 通关率 | 平均阵亡波次 |
|---|---|---|
| 只堆无属性塔（歪路） | **0.0%** | 第 4.1 波 |
| 三属性平均铺，不看预告 | 21.0% | 第 7.9 波 |
| 随机升属性 | 26.3% | 第 8.1 波 |
| **照克制建塔** | **59.3%** | 第 9.2 波 |
| 照克制 + 拆塔重组 | 55.0% | 第 9.0 波 |

目标定在「最强打法约 60%」，靠 `HP_GROWTH` 控制（1.585）。

### 脚本玩家的决策模型改过一次，之前的数字都不可比

原本用一张固定的奖励偏好清单「照顺序挑最靠前的」。加入刷新之后这个模型直接崩了：
AI 会一直刷到清单最前面那张常见 C 卡为止，**通关率从 45% 掉到 1%**。

原因是奖励的叠加规则：**同类加算、跨类相乘**。
27 次全拿「淬火」是 +324%（4.2 倍），拆成伤害＋攻速＋穿透＋暴击分散拿，
是好几个倍率相乘，远比 4.2 倍大。一直刷同一张等于亲手毁掉自己的 build。

解法是给评分加上**边际递减**（同一张拿过越多次，再拿的价值越低），
顺便让经济类奖励的价值随波次递减。改完之后脚本玩家实力大幅提升，
所以这一版之前的所有通关率数字都不能拿来比。

### 三个踩过的坑

**1. 塔太便宜 → 金钱失去意义。**
固定 60 块时第 3 波就填满 8 个塔位，结局剩 2478 金没花。改成递增成本。

**2. 只靠血量几何成长 → 前 9 波白送、第 10 波一堵墙。**
`HP_GROWTH = 1.54` 时前 9 波击杀率 91~100%，第 10 波暴跌到 59%、一口气扣 10.9 命，
而且赢法是「用命硬扛漏怪」而不是打得好。
解法是把血量成长压到 1.50，难度分一半给**数量成长**（每波多 1.6 只）。

**3. 拆塔重建原本是亏的，修了两处才拉平。**
最初 `adaptive`（会拆掉用不上的塔）通关率 34.0%，**反而低于** `counter`（从不拆塔）38.3%。
查下来是两个叠加的损耗：拆塔只返还 60%，以及塔价按「已有塔数」计算导致原位重建要付最贵档。
加上「每波免费拆 1 次」并把定价改绑塔位之后，两者拉平到 37.0% vs 38.3%。
属性重组不再是陷阱，但对脚本 AI 也还谈不上优势——它的拆塔判断只看下一波，
人类有前瞻性应该能打得更好。

### 「选哪个属性」值多少钱

实际试玩后有个反馈：**打起来感觉不到克制有意义**。跑对照组验证：

| 打法 | 通关率 |
|---|---|
| 只堆无属性塔 | 0.0% |
| 随机升属性，完全不看怪 | 19.0% |
| 三属性平均铺，不看预告 | 15.7% |
| **照预告建克制塔** | **38.3%** |

看预告做选择比无脑平均铺**强 2.4 倍**——机制没问题，是**没演出来**。
打中一只怪不管 ×2.6 还是 ×0.3，画面上都是同一条白线同一个音效，
玩家没有任何途径感知它。而且玩家通常一直在正确建塔，从没体验过打错的后果，
一个从没做错过的机制当然感觉不到存在。

解法全部在表现层，一个数值都没动（见下方「命中反馈」）。

### 「战斗中可操作」的代价

开放波次进行中建塔之后，最强打法（照克制建塔＋拆塔重组）从 37% 冲到 50%，
所以把 `HP_GROWTH` 从 1.50 提到 1.52 拉回来。校准后：

| 打法 | 通关率 |
|---|---|
| 只堆无属性塔 | 0.3% |
| 三属性平均铺 | 12.3% |
| 照克制建塔 | 23.3% |
| **照克制＋拆塔重组** | **37.3%** |

这个改动的真正价值不是难度，是**把技术差距拉开了**：以前「照克制建塔」就是天花板，
现在它和「拆塔重组」之间还隔著 14 个百分点，多出一层可以练的东西。

### 歪路封堵

无属性倍率从 0.45 压到 0.30，把「只堆无属性塔」的通关率从 20% 打到 0%。
这是保证「必须靠克制」这条核心设计成立的关键数字。

## 九、表现层

`scenes/main.tscn` + `scripts/ui/game.gd`，设计分辨率 800×560。
全部用 `_draw()` 直接画，没有美术资源依赖。

- 上方 HUD：波次、金钱、生命、下一波属性预告、加成按钮
- 中间赛道：蛇行四趟的折线，怪按属性上色，精英怪带金边和更大体积
- 下方 8 个塔位：点击选中后弹出建造／升级／拆除按钮
- 标题页：开始游戏 / 游戏说明 / 退出游戏
- 奖励阶段：弹窗 + 3 张卡片，左上角**刷新**、右上角可**缩小**成一条横条，
  让玩家看清场上的塔和下一波预告再决定选哪张（缩小后仍然不能操作）
- 加成面板：左栏列「领了哪些奖励各几次」，右栏列「换算成实际效果」
- 说明面板（`?` 按钮）：目标、克制关系、塔与练级、×倍率的意思、技能、奖励稀有度
- HUD 分两行：上行是波次／金钱／生命与功能键，下行是波次预告与技能按钮

> **技能冷却必须走 `RunState.step_battle()`。** 表现层一度直接呼叫 `battle.step()`，
> 结果冷却是在 RunState 这一层递减的，于是永远卡在 18 秒不动，技能一局只能放一次。
> 回归测试里有一条专门盯著这个。
>
> **`×倍率` 要自己解释自己。** 塔位下那个 `×2.60` 玩家第一眼看不懂是什么，
> 建造阶段的提示行现在直接写「这座塔对下一波怪的实际伤害倍率」。
> 技能按钮同理，从 `木 18s` 改成 `木 冷却 18s`，并加了一条会动的进度条。
>
> **缩小奖励弹窗时要记得收掉塔位操作面板。** 那排「火55/木55/拆除」是真的 Button，
> 弹窗一缩遮罩就没了，不主动清掉 `selected_slot` 的话玩家能在奖励阶段偷偷建塔。
> 规则层的阶段判断挡得住 `_unhandled_input`，但挡不住已经存在的 Button。
>
> **加成面板必须盖在奖励遮罩之上。** 一局有 27 次三选一，
> 正在选的时候最需要回头查自己手上已经有什么——如果面板被奖励遮罩盖住，
> 这个功能在最该用的时刻恰好用不了。实作上就是把按钮和面板最后才加进 UI 树。
>
> 另一个原因：加成本来是 HUD 上的一行文字，但 27 次选择叠下来那行永远塞不下，
> 实测第 2 波就被右上角的按钮截断了。
- 战斗阶段可切 1x / 2x / 4x 倍速，且战斗中一样能操作塔位

> **坑：** 战斗中买得起买不起一直在变，但**塔位按钮不能每帧重建**——
> 按下去那一瞬间按钮被 `queue_free`，`pressed` 信号就不会发出来，点击会失灵。
> 只在「选中的塔位／塔的状态」变了才重建，可否点击每帧只改 `disabled`。

单向数据流：界面只读 `RunState` 和 `Battle` 的状态，玩家操作一律走
`RunState` 的方法，规则层不认识画面。弹道靠 `Battle.last_shots` 记录的
「谁打了走到哪里的怪」，怎么画是 UI 自己的事。

### UI 风格系统

所有颜色、圆角、边框、贴图路径、字体、辉光强度都收敛成 `Palette` 的样式令牌，
换风格 = 换一个 preset，`game.gd` 一行绘制代码都不用改。

现有四套：`classic`（最初的深色扁平）、`polish`（程序绘制强化）、
`scifi`（Kenney 九宫格贴图 + 辉光 + 扫描线）、**`pixel`（当前选定）**。

命令行可以直接切：`godot --path . -- scifi`

**贴图和精灵一律先去饱和，运行时用 `modulate` 乘上属性色。**
这样属性颜色完全由代码控制，不会被素材本身的色偏带偏 ——
火/木/水 三色是玩法信息，认错颜色就等于认错属性。

精灵图来自 [Kenney Tiny Dungeon](https://kenney.nl/assets/tiny-dungeon)（CC0，16×16）：
火＝螃蟹、木＝史莱姆、水＝幽灵、精英＝带角的大家伙、塔＝骑士。

> **Kenney 这套像素素材有两个坑，都得在导入时处理掉：**
>
> 1. **深色背景是烘进图里的，不是透明的。** micro-roguelike 的 8×8 图标
>    整块 alpha 都是 255，直接用会在画面上显示成一个个黑方块。
>    解法是从四边洪水填充剥掉背景 —— 只剥跟边缘连通的部分，
>    角色自己的深色描边被本体包住，不会被误伤。
> 2. **描边是死黑的。** 剥完背景还是一个个黑框，因为那是素材自带的粗描边。
>    把亮度 ≤60 的像素改成半透明的灰（`(90,90,90,168)`），
>    乘上属性色之后只像一层影子，在任何底色上都不糊。
>
> 另外轮廓方正的素材要避开 —— 螃蟹那张描边直接围成一个方块，换成蜘蛛才解决。
>
> **挑素材要按「最终显示效果」挑，不是按原图挑。** 精英怪原本选的那张，
> 原图看着挺好，去饱和之后变成一团没细节的色块。后来把候选全部按
> 「去饱和 → 染色 → 放大到实际尺寸」渲染出来并排比较，才选对。

### 打击感

| 事件 | 表现 |
|---|---|
| 塔开火 | 整座塔往下沉 3px（后座）＋ 枪口白色方块火光 |
| 命中 | 弹道末端一个小火花方块，克制命中再炸一圈橙环 |
| 击杀 | 按怪的属性色溅出小方块粒子，抛物线落下（精英怪粒子更多） |
| 灼烧跳伤 | 每次跳伤也会溅粒子 |
| 技能 | 从「放技能的那几座塔」中间升起扩散环，再叠一层淡淡的全屏染色 |

技能特效原本只有全屏染色，问题是它跟「谁放的」没有任何视觉联系。
改成冲击波从该属性塔的枪口中点升起之后，才看得出来是这几座塔干的。
全屏染色也从 0.32 压到 0.18，不然会把冲击波和粒子全盖掉。

> 粒子在像素风下用**方块**不用圆点 —— 圆点在这套风格里是异类。
> 这一条走的是 `Palette.ROUND_SHAPES`，所以换回其他风格会自动变圆点。

### 背景不能是纯色

战场原本是纯黑（`#1a1c2c`），配上亮蓝色的面板，中间那一大块看着像个**洞**。
三处一起改才解决：

1. 背景提亮到 `#232741`，面板压暗到 `#2c3560` —— 两边往中间靠，层次才连得上
2. 战场铺一层极淡的 16px 网格（`Palette.BG_PATTERN`），有「地面」的感觉
3. 塔位底色跟着调亮，别在亮面板上显得像黑洞

> 这不是贴图的问题。换成精灵图之后画面信息密度上去了，
> 原本还能忍的大片纯色背景就显得特别空 —— **加了细节的地方会反衬出没加细节的地方**。

### 几处表现层的小决定

- **金钱和生命用图标代替「金」「命」两个字**（金币来自
  [Kenney Micro Roguelike](https://kenney.nl/assets/micro-roguelike)，CC0，8×8）。
  数字旁边一颗心、一枚金币，比读字快，也更像游戏而不是表格。
  > 第一版选的是一堆碎金币，8px 缩到 20px 之后糊成一块格子布，
  > 换成实心金环才认得出来。**小图标要按最终尺寸挑，不能看原图。**
- **怪旁边不再画元素点。** 精灵图本身的颜色已经代表属性了，
  再加一个小点反而像另一只小怪。
- **塔的等级用方点而不是「Lv3」。** 一眼数得出来比读字快，
  而且暗点还顺带告诉玩家「还能再练几级」。
- **赛道铺了砖缝。** 纯色带子太平，加上横向的缝之后才像一条「路」。
- **奖励卡片顶部加稀有度色条**，卡片底色也往稀有度色偏一点 ——
  比一个孤零零的字母有分量得多。

> **坑：`draw_string` 的 y 是基线不是顶端。** 等级点第一次排在
> 名字上方 6px 的位置，结果直接压在字上。

> **中文字体必须内嵌。** 原本用 `SystemFont` 挂 PingFang SC，
> 结果「杀」(U+6740) 这种极常用字会渲染成空白方块。换成内嵌 Noto Sans SC 之后正常，
> 顺带也解决了「打包发给别人、对方没装字体就满屏方块」的问题。
> 像素风用 Fusion Pixel 12px，必须关掉抗锯齿和次像素定位，否则会糊成一团。

### 命中反馈

克制是这局游戏的胜负手，所以每一次命中都必须说清楚「打对没有」：

| 效果 | 飘字 | 弹道 | 音效 |
|---|---|---|---|
| 克制 | 大字橙色 `克制 26` | 粗光束 + 命中炸一圈橙环 | 清脆高频 |
| 暴击 | 大字紫色 `暴击 52` | 更粗 + 紫环 | 上扬音 |
| 普通 | 中字白色 `12` | 细白线 | 中性短音 |
| 被克／无属性 | 小字灰色 `无效 3` | 极细灰线 | 低频闷响 |

> **踩过的坑 1：** 同一只怪常在同一瞬间被好几座塔打中，飘字全叠在同一个点，
> 「克制 26」和「无效 3」糊成一团乱码。解法是同类命中累加成一个数字，
> 不同类的往上错开一行。随机偏移是不够的。
>
> **踩过的坑 2：** 最初飘字写成 `+26` 而且用金色，结果实际试玩时第一反应是
> 「这是赚了 26 块钱吗」。`+` 看起来像「获得」，而金色跟 HUD 的「金 161」撞色。
> 伤害数字不加正负号、每一档都带文字标签，金色只留给钱。

另外两个常驻指示：

- **塔位下的 ×倍率**：每座塔对当前波次的加权平均倍率，`×2.60` 绿 / `×0.30` 红。
  无属性塔会顶著刺眼的红色低倍率，比写「可升级」三个字更能让玩家自己想去升级。
- **怪旁的小圆点**：标出克制它的属性颜色，玩家不用背三角关系。

效果等级由规则层的 `Types.effectiveness()` 判定，表现层只负责演。
回归测试里有一条断言锁住「等级必须和倍率一致」——判错了反馈就是在骗玩家。

### 音效

`scripts/ui/sfx.gd`，**全部在运行时程序合成**（方波／锯齿／正弦 + 衰减包络），
不依赖任何外部素材——没有下载、没有体积、没有授权问题，想调手感直接改参数。

15 个音效：选中、开火、克制命中、被克命中、击杀、暴击、建塔、升级、拆除、
波次开始、漏怪、奖励、通关、失败。
开火在 4 倍速下会糊成一片，所以同类音效有最小间隔限制。

要换成真素材：把同名文件丢进 `assets/audio/`（`.wav` 或 `.ogg`）就会自动覆盖，
例如 `assets/audio/kill.wav`。代码不用动。

### 截图工具

终端环境截不了屏，`tests/tools/capture.gd` 让游戏自己把各阶段画面存成 PNG：

```bash
godot --path . --script res://tests/tools/capture.gd
```

> **坑：** `root.get_texture()` 拿到的是「上一帧」已经画完的画面。
> 改完状态必须等一帧再截，否则截到的是改之前的样子。

## 十、待办

- [ ] 打包发布前要内嵌中文字型，不能依赖系统字型
- [ ] 拆塔重组目前只是「不亏」，还不是优势玩法，看实际手感再决定要不要加强
- [ ] 合成音效只是能听，手感还糙；要质感就换真素材（见上方音效一节）
- [ ] 打击感：命中闪白、击杀粒子、波次开始的过场
- [ ] 10 波通关之后的内容（跨局解锁／难度层）暂不做
