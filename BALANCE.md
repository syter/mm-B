# 数值存档

**v1.2 快照** · 全部对应 `scripts/core/balance.gd` 与 `scripts/core/modifiers.gd`。

> **调数值之前先看这份。** 每个参数都标了「调它会怎样」和「实测灵敏度」，
> 有些参数看着能调，实际上已经失效或者会连带破坏别的东西。

---

## 一、当前表现（腳本玩家 GreedyPlayer，300 局）

| 打法 | 通关率 | 平均阵亡波次 |
|---|---|---|
| 只堆无属性塔（歪路） | **0.0%** | 第 4.5 波 |
| 三属性平均铺，不看预告 | 54.3% | 第 7.3 波 |
| 随机升属性 | 61.7% | 第 7.8 波 |
| **照克制建塔** | **81.3%** | 第 9.1 波 |
| 照克制 + 拆塔重组 | 78.0% | 第 8.9 波 |

设计目标：最强打法约 60%，歪路必须 0%。

### ⚠️ 未完成：目标通关率 80 / 50 / 30

**当前实际是 58% / 11% / 1%，离目标很远。** 下面是已经跑出来的搜索结果，
明天接着调的时候直接用，不用重新推导。

`REWARD_POWER`（奖励强度全局倍率）× `SKILL_DAMAGE_PER_LEVEL`（大招每级伤害）
各 100 局，「照克制建塔」的通关率：

| 大招/级 | 奖励×1.00 | 奖励×1.35 | 奖励×1.70 |
|---|---|---|---|
| 26 | 58 / 11 / 1 | 94 / 75 / 26 | 99 / 98 / 81 |
| 50 | 64 / 14 / 2 | 97 / 84 / 30 | 99 / 99 / 84 |
| 80 | 67 / 16 / 1 | 96 / 85 / 43 | 99 / 99 / 87 |

（每格是 简单 / 困难 / 地狱）

**三条结论：**

1. **大招伤害不是有效旋钮。** 从 26 翻到 80（三倍多），简单档只动 2 个百分点。
   技能要凑三座同属性才解锁，一局放不了几次，总输出占比太小。
2. **奖励强度是主导旋钮。** 1.00 → 1.35 能把地狱从 1% 拉到 26%。
3. **⚠️ 现在的难度间距（1.5 / 1.55 / 1.6）达不到 80/50/30。**
   看 ×1.35 那行：94 / 75 / 26 —— 简单到困难只差 19 点，困难到地狱差 49 点。
   目标要的是「30 点 + 20 点」的均匀下降。
   血量是复利，第 10 波时 1.6 比 1.5 厚 1.8 倍，这个差距在末波被放大到无法弥补。
   **要达标就得收窄 HP 成长的间距**（比如 1.50 / 1.535 / 1.57），
   而那会改掉用户定的 1.55 / 1.6 —— 需要他拍板。

歪路（只堆无属性塔）在所有组合下都是 0%，核心设计没被破坏。

### 三个难度档位

只动 `HP_GROWTH` 一个参数。它太灵敏不适合微调，但正因为灵敏，拿来做档位反而干净 ——
一个参数就拉开明显的体感差距，而且不牵连经济、塔价这些玩家已经熟悉的数字。

| 难度 | HP成长 | 歪路 | 平均铺 | **照克制** | 拆塔重组 |
|---|---|---|---|---|---|
| 简单 | 1.50 | 0.0% | 58.5% | **82.0%** | 78.0% |
| 困难 | 1.55 | 0.0% | 17.5% | **38.0%** | 31.0% |
| 地狱 | 1.60 | 0.0% | **0.0%** | **7.0%** | 1.5% |

> 地狱档「三属性平均铺」是 0% —— 不看预告就绝无可能通关。
> 歪路（只堆无属性塔）在三个档全是 0%，核心设计没被难度选择破坏。
>
> 想微调难度请用别的参数（`COUNT_PER_WAVE` / `TOWER_BASE_DAMAGE` /
> `TRACK_LENGTH` / 经济），别动 `HP_GROWTH`。

> 这是**腳本 AI** 的成绩，不是人的。这个数字的可信度完全取决于那个 AI 有多像人 ——
> 曾经只改 AI 的奖励决策模型（没动任何游戏数值），通关率从 1% 跳到 97%。

---

## 二、难度旋钮（想改难度先看这里）

| 参数 | 当前值 | 灵敏度 | 备注 |
|---|---|---|---|
| **`HP_GROWTH`** | 由**难度档位**决定 | 极高 | 不要在代码里手改。见下方难度表 |
| `COUNT_PER_WAVE` | 1.6 | 中 | 每波多几只。跟血量搭配用能让难度分布更平均 |
| `HP_BASE` | 55.0 | 中 | 第 1 波单只血量，整体平移难度 |
| `TOWER_BASE_DAMAGE` | 10.0 | 中 | 反向旋钮，跟 HP 是同一个杠杆的两端 |
| `TRACK_LENGTH` | 24.0 | 中 | 怪走完的时间＝塔的输出窗口（约 14 秒） |
| `LEVEL_COST_GROWTH` | 2.0 | **已失效** | 曾经是主力（1.8→70.7%、4.0→39.2%），加了开局奖励和刷新之后失灵（4.0→95.3%、8.5→86.0%）。赏金砍到 3+0.9n 后从 2.6 降到 2.0，否则练级贵到不成立 |

> **`HP_GROWTH` 太灵敏是已知缺陷**：0.06 的差距能把通关率从 85% 打到 33%，
> 不好微调。真要做细致的难度分档，应该先把它拆成「基础血量曲线 + 波次系数表」。

---

## 三、属性克制（核心机制，动它要非常小心）

| 参数 | 当前值 | 说明 |
|---|---|---|
| `MULT_COUNTER` | **3.0** | 克制。火→木→水→火 |
| `MULT_SAME` | 0.55 | 同属性 |
| `MULT_COUNTERED` | **0.1667** | 被克制，克制差距 `3.0 / 0.1667` = **18 倍**。**比无属性（0.30）还低** —— 打错属性比不带属性更惨 |
| **`MULT_NEUTRAL_ATK`** | **0.30** | 无属性塔打有属性怪。**专门堵死「只堆无属性塔」的歪路**，0.45 时那条路通关率 20%，压到 0.30 才降到 0% |

> 回归测试里有一条硬约束：**克制 / 被克 的倍率差必须 ≥ 5 倍**（现在 **12.0 倍**）。
> 低于这个数玩家就不会在意属性，整个核心机制白做。改这四个数会触发那条断言。

---

## 四、经济

| 参数 | 当前值 | 说明 |
|---|---|---|
| `START_GOLD` | 220 | 开局只够 2 座塔 + 1 次升级 |
| `START_LIVES` | 20 | |
| `TOWER_COST` | 60 | 第 1 个塔位 |
| `TOWER_COST_GROWTH` | 1.25 | 塔位价格 = `60 × 1.25^slot` → 60/80/90/120/150/180/230/290，八座共 1200，**绑塔位不绑塔数** |
| `UPGRADE_COST` | 60 | 无属性 → 属性塔 |
| `REFUND_RATE` | 0.6 | 拆塔返还（免费次数用完之后） |
| `FREE_SELLS_PER_WAVE` | 简单 1 / 困难 1 / 地狱 0 | 每波开始补几次免费拆除（0 = 不按波补） |
| `FREE_SELLS_TOTAL` | 简单 −1 / 困难 3 / 地狱 1 | 整局上限（−1 = 不限）。困难每波补 1 但整局只能用 3 次 |
| `BOUNTY_BASE` / `BOUNTY_PER_WAVE` | 3.0 / 0.9 | 击杀赏金 = `round(3 + 0.9×波次)`，逐波 4 5 6 7 8 8 9 10 11 12 |
| `WAVE_CLEAR_GOLD` | 40 | 过波奖金 |

实际价格（都过 `Balance.round_cost()` 取整到 10）：

- 塔位：**60 / 90 / 130 / 180 / 270 / 380 / 560 / 810**
- 升级成属性塔：**60**
- 练级：**120 / 240 / 480**

> **塔价必须绑塔位，不能绑「已有几座塔」。** 绑塔数的话，拆掉早期的便宜塔、
> 原位重建却要付最贵那一档，白亏几百块，属性重组在数值上根本不成立。

---

## 五、塔

| 参数 | 当前值 | 说明 |
|---|---|---|
| `MAX_TOWERS` | 8 | 塔位上限 |
| `TOWER_BASE_DAMAGE` | 10.0 | |
| `TOWER_BASE_RATE` | 1.2 | 每秒攻击次数 |
| `MAX_TOWER_LEVEL` | 4 | 每级多打一个全额伤害目标 |
| `LEVEL_COST_GROWTH` | 2.0 | 练级价格 = `60 × 2.0^(n-1)` → 120 / 240 / 480 |

> **压「等级上限」控难度没用**：Lv3 上限反而比 Lv4 通关率更高（60.7% vs 55.3%），
> 因为省下来的钱会跑去多铺塔。

---

## 六、技能

| 参数 | 当前值 | 说明 |
|---|---|---|
| `SKILL_REQUIRED_TOWERS` | 3 | 同属性凑满几座解锁 |
| `SKILL_COOLDOWN` | 18.0 | 秒。每波开场重置 |
| `SKILL_DAMAGE_PER_LEVEL` | 26.0 | 伤害 = 这个 × 该属性所有塔的**等级总和**，再乘克制倍率 |
| `SKILL_BURN_RATIO` / `_DURATION` / `_TICK` | 0.6 / 3.0 / 0.2 | 火：灼烧总伤是技能伤害的 60%，分摊在 3 秒里每 0.2 秒跳一次 |
| `SKILL_SLOW_PCT` / `_DURATION` | 0.5 / 4.0 | 水：减速 50%，持续 4 秒 |
| `SKILL_ROOT_DURATION` | 2.5 | 木：完全定身 2.5 秒 |

> 实测技能伤害从 10 调到 26，通关率几乎不变 —— **技能不是难度瓶颈**，
> 想控难度别动这里。

---

## 七、波次

| 参数 | 当前值 | 说明 |
|---|---|---|
| `TOTAL_WAVES` | 10 | |
| `HP_BASE` / `HP_GROWTH` | 55.0 / 1.5 | 第 n 波单只血量 = `55 × 1.5^(n-1)`（HP_GROWTH 已冻结） |
| `COUNT_BASE` / `COUNT_PER_WAVE` | 6 / 1.6 | 第 n 波数量 = `6 + 1.6n`（第 1 波 8 只，第 10 波 22 只） |
| `SPAWN_INTERVAL` | 0.75 | 出怪间隔（秒） |
| `ENEMY_BASE_SPEED` / `SPEED_GROWTH` | 1.7 / 0.02 | 每波提速 2% |
| `TRACK_LENGTH` | 24.0 | 约 14 秒走完 |
| `WAVE_TWO_ELEMENTS` / `_THREE_` | 4 / 7 | 第 4 波起两属性混合，第 7 波起三属性 |
| `ELITE_WAVES` | [4, 6, 8] | 属性单独随机，不跟本波杂兵走 |
| `ELITE_HP_MULT` / `_BOUNTY_` / `_SPEED_` | 6.0 / 5.0 / 0.65 | |
| `BOSS_WAVE` / `BOSS_HP_MULT` | 10 / 20.0 | 大 BOSS 压轴 |
| `BOSS_BOUNTY_MULT` / `BOSS_SPEED_MULT` | 12.0 / 0.5 | |
| `LEAK_COST_NORMAL` / `_ELITE` / `_BOSS` | 1 / 5 / 10 | 漏怪扣命 |
| `ELEMENT_SPEED_MULT` | 火 1.28 / 木 0.78 / 水 1.0 | 属性自带的移动速度 |
| `ELEMENT_HP_MULT` | 火 0.82 / 木 1.38 / 水 1.0 | 属性自带的血量 |
| `WATER_DEATH_HEAL` | 0.05 | 水怪死时给全场回血的比例 |

> **只靠血量几何成长会毁掉难度曲线**：`HP_GROWTH = 1.54` 时前 9 波击杀率
> 91~100%（全是白送），第 10 波暴跌到 59%，赢法变成「拿命硬扛漏怪」。
> 必须把一部分难度分给 `COUNT_PER_WAVE`。

---

## 八、奖励

| 参数 | 当前值 | 说明 |
|---|---|---|
| `REWARD_ROUNDS` / `REWARD_CHOICES` | 3 / 3 | 每波 3 次三选一，含开局共 30 次 |
| `REROLL_FREE_PER_ROUND` | 简单 1 / 困难 0 / 地狱 0 | 每次选择补几次免费刷新 |
| `REROLL_FREE_PER_PHASE` | 简单 0 / 困难 1 / 地狱 0 | 每个奖励阶段（3 次选择）给几次免费刷新 |
| `REROLL_BASE_COST` | 20 | 第一次付费刷新的基价 |
| `REROLL_COST_GROWTH` | 2.1 | 同一轮内每多刷一次的倍数 |
| `REROLL_WAVE_SCALE` | 0.1 | 每往后一波，刷新价 +10% |
| 稀有度权重 | C 100 / B 55 / A 28 / S 9 | 同时决定卡片颜色和抽中概率 |

实际刷新价格：

| 波次 | 第1次 | 第2次 | 第3次 | 第4次 | 第5次 |
|---|---|---|---|---|---|
| 1 | 免费 | 20 | 40 | 90 | 190 |
| 5 | 免费 | 30 | 60 | 120 | 260 |
| 10 | 免费 | 40 | 80 | 170 | 350 |

> **封顶的奖励不再出现在三选一里。** 堆满的奖励继续被抽到等于白白浪费一格。
> 暴击不在此列 —— 机率和倍率都不封顶，永远还有得涨。

### 加成封顶（`modifiers.gd`）

| 参数 | 当前值 |
|---|---|
| `CAP_COUNTER_BONUS` | 1.0（克制倍率最高 3.6） |
| `CAP_COUNTERED` | 0.7 |
| `CAP_NEUTRAL` | 0.9 |
| `CAP_SLOW` | 0.6 |
| `CAP_INTEREST` | 0.3 |
| `CAP_UPGRADE_DISCOUNT` | 0.6 |
| `CAP_SKILL_CD` | 0.5 |
| `PIERCE_RATIO` | 0.4 |
| 暴击阶梯 | 机率 `0.20 + 0.15×(n-1)`，只在 100% 处截断；倍率 `2.0 + 0.5×(n-1)`，不封顶 |

> **「淬火」「急速」按属性发，不是全场加成。** 加成挂在 `Modifiers.damage_by_element`
> / `rate_by_element` 上，只作用在中奖的那一种属性的塔，无属性塔一点都吃不到。
> 池子里这两张各只占一格（抽中的当下才掷属性），不然 C 档权重会凭空翻三倍。

> **奖励是「同类加算、跨类相乘」。** 27 次全拿「淬火·火」是 +405%（5.1 倍），
> 拆成伤害＋攻速＋穿透＋暴击分散拿，是几个倍率相乘，远比 5.1 倍大。
> 改任何一条加成的数值前，先想清楚它会怎么跟别的相乘。

---

## 九、怎么验证改动

```bash
godot --headless --path . --script res://tests/sim/sim_run.gd      # 五种打法的通关率，300 局
godot --headless --path . --script res://tests/sim/sim_trace.gd    # 单局逐波明细
godot --headless --path . --script res://tests/sim/sim_power.gd    # 扫参数找目标值
godot --headless --path . --script res://tests/regression/test_core.gd  # 硬约束没被破坏
```

`balance.gd` 里的参数都是 `static var` 不是 `const`，所以模拟脚本能直接扫。
扫完记得 `Balance.reset()` 复位，否则污染下一批。
